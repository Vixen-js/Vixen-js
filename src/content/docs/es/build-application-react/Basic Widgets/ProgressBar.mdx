---
title: ProgressBar
description: Cómo usar ProgressBar en tu aplicación
---

import { Code } from '@astrojs/starlight/components';

> **ProgressBar** es un widget que proporciona una barra de progreso que puedes utilizar en tu aplicación.

## Ejemplo

export const progressBarExample = `
import { ProgressBar } from '@vixen-js/core-react';

export function ProgressBarExample() {
    return (
        <ProgressBar value={50} min={0} max={100} />
    );
}
`;

<Code code={progressBarExample} title='example_progressBar.tsx' lang='tsx' />

Aprende más sobre [`ProgressBar`](https://vixen-js.github.io/docs/build-application-vanilla/Basic-Widgets/QProgressBar).