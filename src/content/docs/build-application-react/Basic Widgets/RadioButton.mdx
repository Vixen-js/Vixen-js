---
title: RadioButton
description: How to use RadioButton in your application
---

import { Code } from '@astrojs/starlight/components';

> **RadioButton** is a widget that provides a native radio button widget with a text label that you can use in your application.

## Example

export const RadioButtonExample = `
import { RadioButton } from '@vixen-js/core-react';

export function RadioButtonExample() {

    const radioEvents = {
        toggled: (checked: boolean) => {
          console.log("Radio button toggled value: ", checked)
        }
    };
    return (
        <RadioButton 
          text="Your radio button"
          on={radioEvents} />
    );
}`;

<Code code={RadioButtonExample} title='example_radio.tsx' lang='tsx' />

Learn more about [`RadioButton`](https://vixen-js.github.io/docs/build-application-vanilla/Basic-Widgets/QRadioButton)